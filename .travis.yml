matrix:
  include:
    - language: rust
      rust:
        - nightly
      script:
        - cargo build --verbose --all
    - language: node_js
      node_js:
        - 8.12
      cache: yarn
      before_install:
        - cd app
      install:
        - yarn install
      script:
        - yarn test
        - yarn build