{
  "name": "pastes.rs",
  "description": "Rust implementation of pastebin",
  "version": "0.1.0",
  "engines": {
    "node": "8.12.0"
  },
  "scripts": {
    "postinstall": "yarn build --preifx app"
  }
}