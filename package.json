{
  "name": "pastes.rs",
  "description": "Rust implementation of pastebin",
  "version": "0.1.0",
  "engines": {
    "node": "8.12.0"
  },
  "dependencies": {
    "yarn": "^1.10.0"
  },
  "scripts": {
    "postinstall": "yarn -cwd app/ build"
  }
}